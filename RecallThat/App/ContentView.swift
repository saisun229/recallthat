import SwiftUI

// Root view — replaced in Phase 1 with real navigation shell
struct ContentView: View {
    var body: some View {
        Text("RecallThat")
            .font(.largeTitle)
            .foregroundStyle(.secondary)
    }
}
